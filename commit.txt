git commit -m 'message'
this command will return the file to the first commit
git reset --hard
if you want to return to spsific commit status use this command

git reset --hard id_of_commit

// if you want to edit last commit in history
git commit --amend

git rebase -i HEAD~3 ==> this mean you will transffer ( HEAD ) to 3 commit in back
